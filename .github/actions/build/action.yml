name: Build
description: Fetch latest data and build the site

runs:
  using: composite
  steps:
    - name: Setup
      uses: ./.github/actions/setup

    - name: Build
      run: pnpm build
      shell: bash
